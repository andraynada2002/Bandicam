1: void foo(){ 
2: int x; 
3: for(int i = 0; i < N; i++) 
4: x += i; 
5: }
void foo(){ 
int x; 
for(int i = 0; i < N; i++) 
x += bar(i); 
}
int bar(int i){ 
return i; 
}
for(int i = 0; i < N; i++) 
*a++ = *b++ + *c++; 
for(int i = 0; i < N; i++) 
a[i] = b[i] + c[i]; 
a += N; 
b += N; 
c += N;
for(int i = 0; i < N; i++) 
a[i] = b[i] + c[i];
public class MP4generator;
public class Neurons;
operator Neurons;
friend Neurons;
register Neurons;
{
	//Bandicam video optimized
	//Bandicam application optimized
	//Bandicam video recording optimized
	//Bandicam video recording realtime performance
	//Bandicam video recording realtime framerate
	//Bandicam video recording realtime fps
	//Bandicam video recording quality Good + Best
	//Bandicam video recording fixed and remove artifacts
	//Bandicam video recording fixed and remove noise
	//Bandicam video recording fixed and remove chromatic aberration
	//Bandicam video recording video Ultra Hight Definition
	propertyName Bandicam;
	propertyName Application;
	propertyName video;
	propertyName recording;
	propertyName realtime;
	propertyName Noise;
	propertyName artifacts
	propertyName ChromaticAberration; propertyName Fixed; propertyName performance; propertyName Framerate; propertyName Fps; propertyName UltraHightDefinition;
}
abort Neurons;
abs Neurons;
absread Neurons;
abswrite Neurons;
asm Neurons;
if Neurons;
else Neurons;
